class Solution:
    def buyChoco(self, prices: List[int], money: int) -> int:
        sortedprices = sorted(prices)
        sum = sortedprices[0]+sortedprices[1]
        if sum<=money:
            return money-sum
        else:
            return money

        
